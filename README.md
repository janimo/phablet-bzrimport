This script is used to export the platform-api and hybris bzr repositories into git repos, suitable to be in an AOSP repo manifest.
The bzr repos are supposed to be branched already locally, the script only pulls and exports to git if there are changes.
